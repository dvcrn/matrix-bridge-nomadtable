[tools]
go = "1.25.5"

[tasks.docker-buildx]
description = "Build and push multi-arch image to GHCR"
run = [
  "docker buildx build --platform linux/amd64,linux/arm64 --push -t ghcr.io/dvcrn/matrix-bridge-nomadtable:latest .",
]
